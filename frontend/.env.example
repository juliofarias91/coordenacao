# Vazio = usa o proxy do Vite (/api → http://localhost:8000).
# Preencha só quando a API estiver em outro host.
#
# ⚠ DEIXE COMENTADA para usar o proxy. Declará-la VAZIA já derrubou o login em
# 07/08/2026: `api.ts` usava `??`, e string vazia não é nulo — o prefixo
# `/api/v1` sumia de toda chamada. Hoje o código usa `||` e tolera, mas ausente
# é inequívoco.
# VITE_API_URL=

# NÃO HÁ MAIS VARIÁVEL DE E-MAIL AQUI (07/08/2026). O convite saía do navegador
# por EmailJS; passou a sair do servidor, por SMTP. Ver `docs/EMAIL.md`.
